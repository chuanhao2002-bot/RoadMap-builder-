import type { ProjectPriority, ProjectStatus } from "@/types/project";

export const STATUSES: ProjectStatus[] = ["Planning", "In Progress", "Blocked", "Completed"];
export const PRIORITIES: ProjectPriority[] = ["Low", "Medium", "High", "Critical"];
